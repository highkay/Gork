// Package config contains configuration loading, ConfigSnapshot snapshots, and
// backend integration helpers that persist runtime overrides.
//
// The package-level API mirrors Python app.platform.config package boundary
// from the empty app/platform/config/__init__.py marker.
package config
